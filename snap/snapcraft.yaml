name: flutter_app
version: 1.0.0
summary: Generated by Icons Launcher
description: Generated by Icons Launcher package for Linux.

confinement: strict
base: core22
grade: stable

slots:
  dbus-flutter_app: # adjust accordingly to your app name
    interface: dbus
    bus: session
    name: org.bar.flutter_app # adjust accordingly to your app name

apps:
  flutter_app:
    command: flutter_app
    extensions: [gnome] # gnome includes the libraries required by flutter
    plugs:
    - network
    slots:
      - dbus-flutter_app
parts:
  flutter_app:
    source: .
    plugin: flutter
    flutter-target: lib/main.dart # The main entry-point file of the application
